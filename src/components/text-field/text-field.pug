label
  | #{field.name}
  button.updateButton edit

input.xpath(type='hidden' value='#{field.path}')

if editing
  p
    | Move the mouse to highlight the text to select, then click when it surrounds just the
    |  #{field.name} of the recipe.
else
  .value #{value}
